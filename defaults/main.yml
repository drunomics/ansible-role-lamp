# General settings
---

# apache
apache_document_root: /srv/default/web
apache_server_admin: webmaster@example.com
apache_server_name: example.com

# Default Password = user
apache_htpasswd:
 - filename: default
   user: user
   password: "$apr1$v9CvCAag$Ow.1dk44cqh5cd87FYE6M1"

# Default Password = admin
adminer_htpasswd:
 - filename: phpmyadmin
   user: admin
   password: "$apr1$fOAE1KZ5$q.V9IEWQ8hbndyNhvL92q0"

# Default Password = project
project_htpasswd:
 - filename: default
   user: project
   password: "$apr1$xOTv52Od$BdcK6nL0uhQWjA.z4ShO0."

# The filename (without the directory path) of the htpasswd file.
# To disable password protection just set it empty.
#apache_htpasswd_file: default
apache_htpasswd_file:
apache_htpasswd_bypass_from: "127.0.0.1"

# Whether PHPmyadmin should be installed.
lamp_adminer: true


# Provide backwards compatibility with old projects.
phpmyadmin_htpasswd: "{{ adminer_htpasswd }}